import { marbles } from 'rxjs-marbles/jasmine';

import { interval } from 'rxjs';
import { map, take } from 'rxjs/operators';

xdescribe('Some RxJS Tests', () => {

  it('should get 10 items in 10 seconds', marbles(m => {

  }));

});
